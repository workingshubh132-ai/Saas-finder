node_modules/
dist/
coverage/

# Environment
.env
.env.local

# SQLite databases (dev/test only — never commit data)
*.db
*.db-journal
prisma/dev.db
prisma/test.db

# Editor/OS
.DS_Store
*.log

# M6 — factory workspaces (docs/M6_ARCHITECTURE_PROPOSAL.md §10): disposable,
# generated product code, never committed to VentureForge's own repository.
factory-workspaces/
